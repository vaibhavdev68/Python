# Write a program to find area of triangle

base = float(input("Enter the value of base: "))
height = float(input("Enter the value of height: "))

area = print(f"Area of triangle : {0.5 * base * height}")